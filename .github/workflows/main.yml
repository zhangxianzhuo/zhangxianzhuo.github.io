name: everydayTask

on:
  workflow_dispatch:
  push:
    branches:
      - main
  schedule:
    # - cron: '*/10 * * * *'
    - cron: '0 15 * * *'

jobs:
  test:
    runs-on: ubuntu-22.04
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: stepOne
        run: |
          date_recite=date -d '1day' +%F
          content_recite=$(cat everydayRecite.csv | grep ${date_recite})
      - name: deploy
        run: |
          git config user.email "zhangxianzhuo@hotmail.com"
          git config user.name "zhangxianzhuo"
          git pull
          date > file.txt
          git add .
          git commit -m "update content"
          git push
